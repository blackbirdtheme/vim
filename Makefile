# Install a local copy
install:
	cp ./colors/blackbird.vim ~/.config/nvim/colors/blackbird.vim

# Uninstall a local copy
uninstall:
	rm ~/.config/nvim/colors/blackbird.vim

# Get the local version and copy it to this repo
get:
	cp ~/.config/nvim/colors/blackbird.vim ./colors/blackbird.vim
